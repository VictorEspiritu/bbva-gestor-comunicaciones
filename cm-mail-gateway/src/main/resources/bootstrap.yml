
server:
  port: 7015
  
management:
  security:
    enabled: false
    
spring: 
  application:
    name: mail-gateway-service
  cloud:
    config:
      uri: http://localhost:7888

eureka: 
  client:
    serviceUrl:
      defaultZone: http://localhost:7991/eureka/
